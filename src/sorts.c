#include "sorts.h"

struct BubbleSort {

};

BubbleSort* CreateBubbleSort() {
    BubbleSort* sortData = NULL;
    return  sortData;
}

int BubbleSortOnData(BubbleSort* _bubbleSortData, void** _dataToSort) {
    if (NULL == _bubbleSortData || NULL == _dataToSort) {
        return -1;
    }

    
}